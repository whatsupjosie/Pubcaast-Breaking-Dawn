[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
# pytest-asyncio is a hard requirement of this suite: eight tests are async and
# silently reported as failures without it. It was neither declared in
# requirements nor configured here, so a clean checkout could not reproduce the
# suite's real result.
asyncio_mode = auto
