# PubCast AI — test dependencies
# The suite in tests/ contains async tests that pytest reports as failures
# without this plugin. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
