[build-system]
requires=['setuptools>=68']
build-backend='setuptools.build_meta'
[project]
name='pubpartner-federation'
version='0.2.0'
dependencies=['fastapi>=0.110','uvicorn>=0.29','httpx>=0.27']
[tool.pytest.ini_options]
testpaths=['tests']
